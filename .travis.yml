sudo: false
language: rust
rust:
  - nightly
  - beta
  - stable
script:
  - cargo build --features=ascii &&
  - cargo test --features=ascii
  - cargo doc --features="ascii image"
# addons:
#   apt:
#     packages:
#     - libcurl4-openssl-dev
#     - libelf-dev
#     - libdw-dev
#after_success:
#- travis-cargo --only stable doc-upload
#- env COVERAGE_TESTING=1 travis-cargo coveralls --no-sudo
notifications:
  email:
    on_success: never
env:
  global:
  - TRAVIS_CARGO_NIGHTLY_FEATURE=dev
  # - secure:
